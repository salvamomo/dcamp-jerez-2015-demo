<?php
/**
 * @file
 * TODO Auto generated file, add description.
 */


/**
 * TODO Autogenerated function.
 */
function dieta_personal_calculadora_dieta_page() {
  $params = array();

  return drupal_get_form('dieta_personal_calculadora_dieta_form', $params);
}

/**
 * Form build callback for calculadora_dieta multistep form.
 *
 * @param array $form
 *   Drupal form array.
 * @param array $form_state
 *   Drupal form_state array.
 * @param mixed $params
 *   Additional params passed into form steps.
 *
 * @return array
 *   Build Drupal form array.
 */
function dieta_personal_calculadora_dieta_form($form, &$form_state, $params) {
  return mforms_build('calculadora_dieta', '_dieta_perfil_fisico', $form_state, $params);
}

/**
 * Form validate callback for calculadora_dieta multistep form.
 *
 * @param array $form
 *   Drupal form array.
 * @param array $form_state
 *   Drupal form_state array.
 */
function dieta_personal_calculadora_dieta_form_validate($form, &$form_state) {
  mforms_validate('calculadora_dieta', $form, $form_state);
}

/**
 * Form submit callback for calculadora_dieta multistep form.
 *
 * @param array $form
 *   Drupal form array.
 * @param array $form_state
 *   Drupal form_state array.
 */
function dieta_personal_calculadora_dieta_form_submit($form, &$form_state) {
  mforms_submit('calculadora_dieta', $form, $form_state);
}
